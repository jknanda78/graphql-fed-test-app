type Article {
  id: ID!
  title: String!
  user: User! @provides(fields: "email name createdAt")
}

input ArticleInput {
  title: String!
}

type Query {
  article(articleId: ID!): Article
  articles: [Article]!
}

type Mutation {
  createArticle(input: ArticleInput!): Article
}
