export type ArticleTable = {
    articleId: string;
    title: string;
}

export type Message = {
    msg: string;
    error: boolean;
    code: string;
};

export type Article = {
    articleId: string;
    title: string;
}
    